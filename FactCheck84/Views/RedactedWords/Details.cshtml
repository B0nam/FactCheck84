@model FactCheck84.Models.RedactedWord

@{
    ViewData["Title"] = "Details";
}

<style>
    .base-form {
        background-color: #222;
        padding: 20px;
        border-radius: 30px;
        font-size: 15px;
    }

    /* Estilize a caixa de conteúdo (textarea) */
    .content-textarea {
        height: 300px; /* Ajuste a altura conforme necessário */
    }

    /* Centralize os elementos do formulário */
    .center-form {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    /* Aumente a largura do campo de entrada */
    .wide-input {
        width: 100%;
    }
</style>

<h1>Details</h1>


<div class="base-form">

    <h4>RedactedWord</h4>
    <hr />
    <div class="col-md-8">
        <dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Word)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Word)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.NewWord)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.NewWord)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.IsHidden)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.IsHidden)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.CreationDate)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.CreationDate)
            </dd>
        </dl>
    </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
